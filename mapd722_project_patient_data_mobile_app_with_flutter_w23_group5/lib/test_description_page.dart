import 'package:flutter/material.dart';

class TestDescritionPage extends StatelessWidget {
  const TestDescritionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Patient Details'),
        centerTitle: true,
      ),
    );
  }
}